'use strict';

const chalk = require(`chalk`);

const errorTheme = chalk.red;
const successTheme = chalk.green;
const versionTheme = chalk.blue;
const helpTheme = chalk.grey;

module.exports = {
  errorTheme,
  successTheme,
  versionTheme,
  helpTheme,
};
